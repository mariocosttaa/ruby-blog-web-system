<nav class="fixed top-0 left-0 right-0 z-40 bg-purple-600 shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-12">
      <!-- Logo -->
      <div class="flex items-center">
        <%= link_to panel_dashboard_index_path, class: "flex items-center space-x-2" do %>
          <div class="w-6 h-6 bg-white rounded flex items-center justify-center">
            <span class="text-purple-600 font-bold text-sm">A</span>
          </div>
          <span class="text-white font-bold text-lg font-title">MarioBlog Panel</span>
        <% end %>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-1">
        <%= link_to 'Dashboard', panel_dashboard_index_path, 
            class: "px-2 py-1 rounded text-xs font-medium transition-colors duration-200 #{nav_active_class('panel/dashboard') ? 'bg-purple-700 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Posts', panel_posts_index_path, 
            class: "px-2 py-1 rounded text-xs font-medium transition-colors duration-200 #{nav_active_class('panel/posts') ? 'bg-purple-700 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Categories', panel_categories_index_path, 
            class: "px-2 py-1 rounded text-xs font-medium transition-colors duration-200 #{nav_active_class('panel/categories') ? 'bg-purple-700 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Tags', panel_tags_index_path, 
            class: "px-2 py-1 rounded text-xs font-medium transition-colors duration-200 #{nav_active_class('panel/tags') ? 'bg-purple-700 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Comments', panel_comments_index_path, 
            class: "px-2 py-1 rounded text-xs font-medium transition-colors duration-200 #{nav_active_class('panel/comments') ? 'bg-purple-700 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
      </div>

      <!-- Right side actions -->
      <div class="flex items-center space-x-4">
        <!-- Theme Toggle -->
        <button id="theme-toggle-btn" class="p-1 rounded text-purple-100 hover:bg-purple-500 hover:text-white transition-colors duration-200" title="Toggle theme">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
        </button>

        <!-- View Site Link -->
        <%= link_to public_home_index_path, 
            class: "px-2 py-1 rounded text-xs font-medium text-purple-100 hover:bg-purple-500 hover:text-white transition-colors duration-200", 
            target: "_blank" do %>
          <svg class="w-3 h-3 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
          </svg>
          View Site
        <% end %>

        <!-- User Menu -->
        <div class="relative">
          <div class="flex items-center space-x-1">
            <div class="w-6 h-6 bg-purple-500 rounded-full flex items-center justify-center">
              <span class="text-white text-xs font-medium">A</span>
            </div>
            <span class="text-purple-100 text-xs font-medium hidden sm:block">Admin User</span>
          </div>
        </div>

        <!-- Logout -->
        <%= button_to destroy_user_session_path, method: :delete, 
            class: "px-2 py-1 rounded text-xs font-medium text-purple-100 hover:bg-red-500 hover:text-white transition-colors duration-200" do %>
          <svg class="w-3 h-3 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
          Logout
        <% end %>

        <!-- Mobile menu button -->
        <button id="mobile-menu-btn" class="md:hidden p-2 rounded-md text-purple-100 hover:bg-purple-500 hover:text-white transition-colors duration-200">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden md:hidden">
      <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3 bg-purple-700">
        <%= link_to 'Dashboard', panel_dashboard_index_path, 
            class: "block px-3 py-2 rounded-md text-base font-medium #{nav_active_class('panel/dashboard') ? 'bg-purple-600 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Posts', panel_posts_index_path, 
            class: "block px-3 py-2 rounded-md text-base font-medium #{nav_active_class('panel/posts') ? 'bg-purple-600 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Categories', panel_categories_index_path, 
            class: "block px-3 py-2 rounded-md text-base font-medium #{nav_active_class('panel/categories') ? 'bg-purple-600 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Tags', panel_tags_index_path, 
            class: "block px-3 py-2 rounded-md text-base font-medium #{nav_active_class('panel/tags') ? 'bg-purple-600 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'Comments', panel_comments_index_path, 
            class: "block px-3 py-2 rounded-md text-base font-medium #{nav_active_class('panel/comments') ? 'bg-purple-600 text-white' : 'text-purple-100 hover:bg-purple-500 hover:text-white'}" %>
        <%= link_to 'View Site', public_home_index_path, 
            class: "block px-3 py-2 rounded-md text-base font-medium text-purple-100 hover:bg-purple-500 hover:text-white", 
            target: "_blank" %>
        <button id="mobile-theme-toggle-btn" class="block w-full text-left px-3 py-2 rounded-md text-base font-medium text-purple-100 hover:bg-purple-500 hover:text-white">
          <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
          Toggle Theme
        </button>
      </div>
    </div>
  </div>
</nav>


<div class="pt-10"></div>