<nav class="fixed top-0 w-full z-50 bg-card/95 backdrop-blur-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <div class="flex items-center space-x-2">
        <%= link_to "MarioTechPortal", root_path, class: "text-xl font-bold text-purple" %>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8">
        <%= link_to "Home", root_path, class: "#{nav_active_class('public/home', nil, 'text-purple font-bold') || 'text-secondary'} hover:text-purple transition-colors duration-200 relative group" %>
        <%= link_to "Articles", public_articles_index_path, class: "#{nav_active_class('public/articles', nil, 'text-purple font-bold') || 'text-secondary'} hover:text-purple transition-colors duration-200 relative group" %>
        <%= link_to "Search", public_search_index_path, class: "#{nav_active_class('public/search', nil, 'text-purple font-bold') || 'text-secondary'} hover:text-purple transition-colors duration-200 relative group" %>
        <%= link_to "Tags", public_tags_index_path, class: "#{nav_active_class('public/tags', nil, 'text-purple font-bold') || 'text-secondary'} hover:text-purple transition-colors duration-200 relative group" %>
        <%= link_to "Categories", public_categories_index_path, class: "#{nav_active_class('public/categories', nil, 'text-purple font-bold') || 'text-secondary'} hover:text-purple transition-colors duration-200 relative group" %>

        <% if session[:user_id].present? %>
          <%= link_to "Login", new_user_session_path, class: "text-secondary hover:text-purple transition-colors duration-200 relative group" %>
        <% else %>
          <%= link_to "Admin Panel", panel_dashboard_index_path, class: "text-secondary hover:text-purple transition-colors duration-200 relative group" %>
        <% end %>

        <!-- Theme Toggle -->
        <button id="theme-toggle-btn" class="theme-toggle-btn p-2 rounded-lg bg-card hover:bg-purple/20 transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 18a6 6 0 100-12 6 6 0 000 12zm0-2a4 4 0 110-8 4 4 0 010 8zM11 1h2v3h-2V1zm11 11h3v2h-3v-2zm-11 10h2v3h-2v-3zM1 12H4v2H1v-2zm19.071-7.071l-2.122-2.122 1.414-1.414 2.122 2.122-1.414 1.414zM4.929 19.071l-2.122-2.122 1.414-1.414 2.122 2.122-1.414 1.414zM4.929 4.929L2.807 7.05 4.22 8.464l2.122-2.122-1.414-1.413zm14.142 14.142l-2.122-2.122 1.414-1.414 2.122 2.122-1.414 1.414z"/>
          </svg>
        </button>
      </div>

      <!-- Mobile Menu Button -->
      <button id="mobile-menu-btn" class="md:hidden p-2 rounded-lg bg-card hover:bg-purple/20 transition-colors">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>
</nav>

<!-- Mobile Menu -->
<div id="mobile-menu" class="hidden md:hidden fixed top-16 left-0 w-full bg-black/50 backdrop-blur-xl border-b border-theme z-40">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
    <%= link_to "Home", root_path, class: "#{nav_active_class('public/home', nil, 'text-purple font-bold') || 'text-secondary'} block py-2 hover:text-purple transition-colors" %>
    <%= link_to "Articles", public_articles_index_path, class: "#{nav_active_class('public/articles', nil, 'text-purple font-bold') || 'text-secondary'} block py-2 hover:text-purple transition-colors" %>
    <%= link_to "Search", public_search_index_path, class: "#{nav_active_class('public/search', nil, 'text-purple font-bold') || 'text-secondary'} block py-2 hover:text-purple transition-colors" %>
    <%= link_to "Tags", public_tags_index_path, class: "#{nav_active_class('public/tags', nil, 'text-purple font-bold') || 'text-secondary'} block py-2 hover:text-purple transition-colors" %>
    <%= link_to "Categories", public_categories_index_path, class: "#{nav_active_class('public/categories', nil, 'text-purple font-bold') || 'text-secondary'} block py-2 hover:text-purple transition-colors" %>
    <button id="mobile-theme-toggle-btn" class="theme-toggle-btn block py-2 text-secondary hover:text-purple transition-colors">Toggle Theme</button>
  </div>
</div>

<div class="pt-20"></div>